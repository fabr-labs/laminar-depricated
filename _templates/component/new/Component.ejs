---
to: <%= app %>/src/components/<%= h.inflection.camelize(name) %>/<%= h.inflection.camelize(name) %>.vue
---
<template>
  <div id="<%= h.inflection.camelize(name) %>">
   
  </div>
</template>

<script>

/* global GlobalEventBus */

export default {
  name: '<%= h.inflection.camelize(name) %>',
  mounted() {
    // GlobalEventBus.$on('...', () => {
    //   ...
    // });
  }
}
</script>

<!-- Add "scoped" attribute to limit CSS to this component only -->
<style scoped>
  #<%= h.inflection.camelize(name) %> {

  }
</style>